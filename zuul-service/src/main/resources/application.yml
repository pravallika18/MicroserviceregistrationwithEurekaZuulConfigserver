zuul:
  prefix: /api
  routes: 
   Go-places:
      path: /User/**
      serviceId: hackathonService 
      
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
  instance:
    hostname: localhost
    
server:
  port: 8079  
  
spring:
  application:
    name: zuul-service  
    
    
    
    
  